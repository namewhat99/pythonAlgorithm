모둘 : 각종 변수, 함수, 클래스를 담고있는 파일
패키지 : 여러 모듈을 묶은 것
* 모듈은 import로 가져올 수 있다.
ex) import math
  math.pi -> 3.141592
  math.sqrt(4.0) -> 2.0
  math.sqrt(2.0) -> 1.414..
 
* 모듈 이름 지정하기
import math as m -> math 모듈의 이름을 m 으로 지정
m.sqrt(4.0) -> 2.0
m.sqrt(2.0) -> 1.414..

* from import
from 모듈 import 변수/ 함수 / 클래스
ex) from math import pi
  pi -> 3.141592..
    from math import sqrt
  sqrt(4.0) -> 2.0
  sqrt(2.0) -> 1.414..
      
