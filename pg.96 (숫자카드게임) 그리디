<<   크게 어려운 문제 X  >>

n,m = list(map(int, input().split()));
arr = [0 for _ in range(n)];
max = 0;
for i in range(n):
  arr[i] = list(map(int, input().split()));
  if min(arr[i]) > max:
    max = min(arr[i]);

print(max);

* 이차원 배열을 입력받으면서 풀 필요는 없었음 *
  --> 이차원 배열 입력받는법 공부함
  
   1. arr = [0 for _ in range (n)];
      for i in range(n):
        arr[i] = list(map(int, input().split()));
        
   2. arr = [];
      for i in range(n):
        arr.append(list(map(int,input().split()));
        
   3. arr = [list(map(int, input().split())) for _ in range(n)] --> 리스트 컴프리헨션 사용
  
